export default function Footer() {
    return (
        <footer className="border-t border-graphite p-6 font-display text-center text-sm text-glacier">
            © {new Date().getFullYear()} Rafala.xyz — unbreakable promises.
        </footer>
    );
}
